# AI Agent 可视化编排工具 - V2 架构设计

> 版本: v2.0  
> 日期: 2026年2月  
> 状态: 架构重构设计  
> 核心变更: 从"人工编排"到"AI生成+人工审核"

---

## 1. 核心设计变更

### 1.1 交互模式对比

```
V1 (当前):                    V2 (目标):
┌─────────────┐              ┌─────────────────────────────┐
│ 用户画流程图 │              │ 用户输入自然语言需求        │
│   (手动)    │              │      ↓                      │
│      ↓      │              │ [Master Agent] 分析         │
│  执行引擎   │              │      ↓                      │
└─────────────┘              │ 生成: 角色团队 + 工作流     │
                             │      ↓                      │
                             │ 用户审核 + 修改            │
                             │      ↓                      │
                             │  执行引擎                   │
                             └─────────────────────────────┘
```

### 1.2 四大决策点映射

| 决策点 | 设计体现 |
|--------|---------|
| **AI生成+用户审核** | MasterAgent → WorkflowGenerator → ReviewQueue → 可编辑画布 |
| **三分屏界面** | ChatPanel(左上) + Canvas(左下) + FileManager(右) |
| **角色系统** | RoleTemplate(预定义) + DynamicAgent(动态创建) |
| **事前/事中干预** | Pre-execution Review(Phase 1) + Runtime Intervention(Phase 2) |

---

## 2. 新架构全景

### 2.1 系统分层

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户交互层 (UI Layer)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────┬─────────────────────────────────────┐ │
│  │     左上: Agent 群聊面板          │      右侧: 项目文件管理            │ │
│  │  ┌────────────────────────────┐  │  ┌────────────────────────────────┐ │ │
│  │  │  💬 Agent Team Chat        │  │  │  📁 Project Files             │ │ │
│  │  │  ┌────────┐ ┌────────┐    │  │  │  ├── 📄 PRD.md                │ │ │
│  │  │  │🤖 CTO │ │📝 产品  │ ...│  │  │  ├── 📄 Architecture.md       │ │ │
│  │  │  └────────┘ └────────┘    │  │  │  ├── 📁 src/                  │ │ │
│  │  │                             │  │  │  └── 📁 tests/                │ │ │
│  │  │ [群聊消息历史...]           │  │  │                               │ │ │
│  │  └────────────────────────────┘  │  └────────────────────────────────┘ │ │
│  ├──────────────────────────────────┤                                     │ │
│  │     左下: 工作流画布              │  ┌────────────────────────────────┐ │ │
│  │  ┌────────────────────────────┐  │  │  📊 实时执行状态               │ │ │
│  │  │  🎨 Workflow Canvas        │  │  │  ├── 当前: Agent-前端 运行中   │ │ │
│  │  │                            │  │  │  ├── 进度: 3/5 节点完成        │ │ │
│  │  │  ┌──────┐  ┌────────┐     │  │  │  └── 耗时: 2m 30s             │ │ │
│  │  │  │Start │──│ Agent1 │     │  │  └────────────────────────────────┘ │ │
│  │  │  └──┬───┘  └───┬────┘     │  │                                     │ │
│  │  │     └──────────┘          │  │  ┌────────────────────────────────┐ │ │
│  │  └────────────────────────────┘  │  │  ⚙️ 操作按钮                    │ │ │
│  └──────────────────────────────────┘  │  ├── [批准并执行]               │ │ │
│                                        │  ├── [修改工作流]               │ │ │
│                                        │  └── [中断执行]                 │ │ │
│                                        │  └────────────────────────────────┘ │ │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
┌───────────────────────────────────┼─────────────────────────────────────────┐
│                              核心服务层 (Core Services)                       │
├───────────────────────────────────┼─────────────────────────────────────────┤
│                                   │                                         │
│  ┌────────────────────────────────┴─────────────────────────────────────┐   │
│  │                     Master Agent 调度中心                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 需求解析器  │  │ 角色规划器  │  │ 任务拆分器  │  │ 依赖分析器  │ │   │
│  │  │(Requirement)│  │(RolePlanner)│  │(Decomposer) │  │(Dependency) │ │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘ │   │
│  │         └─────────────────┴─────────────────┴─────────────────┘      │   │
│  │                              ↓                                        │   │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │   │
│  │  │              Workflow Generator (工作流生成器)                   │ │   │
│  │  │  - 将角色和任务转换为可执行DAG                                   │ │   │
│  │  │  - 生成初始节点布局                                              │ │   │
│  │  └─────────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                   │                                         │
│  ┌────────────────────────────────┴─────────────────────────────────────┐   │
│  │                     Agent Runtime (运行时)                            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 角色实例池  │  │ IM消息总线  │  │ 状态管理器  │  │ 上下文管理  │ │   │
│  │  │(RolePool)   │  │(MessageBus) │  │(StateMgr)   │  │(ContextMgr) │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                   │                                         │
│  ┌────────────────────────────────┴─────────────────────────────────────┐   │
│  │                     Execution Engine (执行引擎)                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 工作流解析  │  │ 调度器      │  │ 节点执行器  │  │ 检查点管理  │ │   │
│  │  │(Parser)     │  │(Scheduler)  │  │(Executor)   │  │(Checkpoint) │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
┌───────────────────────────────────┴─────────────────────────────────────────┐
│                              数据存储层 (Data Layer)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │ PostgreSQL  │  │    Redis    │  │   MinIO     │  │  WebSocket  │        │
│  │ - Workflows │  │ - IM Messages│  │ - Files     │  │ - Realtime  │        │
│  │ - Agents    │  │ - States     │  │ - Artifacts │  │ - Chat      │        │
│  │ - Users     │  │ - Sessions   │  │ - Checkpoints│ │ - Status    │        │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 关键模块详细设计

### 3.1 Master Agent 模块

```typescript
// MasterAgent 核心职责
class MasterAgent {
  // 1. 需求解析
  async parseRequirement(input: string): RequirementAnalysis {
    // - 识别任务类型 (WebApp/Backend/DataPipeline/...)
    // - 提取功能要点
    // - 估算复杂度
  }

  // 2. 角色规划
  async planRoles(requirement: Requirement): RoleAssignment[] {
    // - 根据需求选择角色组合
    // - CTO: 架构设计
    // - PM: PRD产出
    // - 前端/后端/QA: 实现
  }

  // 3. 任务拆分
  async decomposeTasks(roles: Role[]): TaskGraph {
    // - 每个角色的子任务
    // - 任务间依赖关系
    // - 并行/串行判定
  }

  // 4. 生成工作流
  async generateWorkflow(tasks: TaskGraph): WorkflowDSL {
    // - 转换为ReactFlow节点
    // - 自动布局
    // - 生成初始配置
  }
}
```

#### 角色模板系统

```typescript
// 预定义角色模板
const ROLE_TEMPLATES: Record<string, RoleTemplate> = {
  CTO: {
    name: 'CTO',
    icon: '🤖',
    systemPrompt: `你是CTO，负责架构设计和技术决策...
      1. 分析需求并设计系统架构
      2. 选择技术栈
      3. 拆分模块和接口定义`,
    capabilities: ['architecture', 'tech_selection', 'module_design'],
    outputArtifacts: ['Architecture.md', 'TechStack.md', 'API.md'],
  },
  
  PRODUCT_MANAGER: {
    name: '产品经理',
    icon: '📝',
    systemPrompt: `你是产品经理，负责需求分析和PRD编写...`,
    capabilities: ['requirement_analysis', 'prd_writing', 'user_story'],
    outputArtifacts: ['PRD.md', 'UserStories.md'],
  },
  
  FRONTEND_DEV: {
    name: '前端开发',
    icon: '💻',
    systemPrompt: `你是前端开发工程师，负责UI实现...`,
    capabilities: ['ui_development', 'component_design', 'state_management'],
    outputArtifacts: ['src/components/', 'src/pages/'],
  },
  
  BACKEND_DEV: {
    name: '后端开发',
    icon: '⚙️',
    systemPrompt: `你是后端开发工程师，负责API实现...`,
    capabilities: ['api_development', 'database_design', 'business_logic'],
    outputArtifacts: ['src/api/', 'src/models/'],
  },
  
  QA_ENGINEER: {
    name: 'QA工程师',
    icon: '🧪',
    systemPrompt: `你是QA工程师，负责测试...`,
    capabilities: ['test_design', 'test_implementation', 'bug_report'],
    outputArtifacts: ['tests/', 'TestPlan.md'],
  },
  
  CODE_REVIEWER: {
    name: '代码评审',
    icon: '👀',
    systemPrompt: `你是代码评审专家，负责代码质量...`,
    capabilities: ['code_review', 'best_practices', 'refactoring'],
    outputArtifacts: ['ReviewComments.md'],
  },
};
```

### 3.2 Agent Runtime (运行时)

```typescript
// Agent 自运行循环
class AgentRuntime {
  private agent: Agent;
  private messageBus: IMessageBus;
  private state: AgentState;

  // 核心循环
  async run() {
    while (this.state !== 'terminated') {
      // 1. 获取未读消息
      const messages = await this.messageBus.getUnread(this.agent.id);
      
      // 2. 处理消息
      for (const msg of messages) {
        await this.processMessage(msg);
      }
      
      // 3. 检查是否有任务可执行
      const task = await this.getPendingTask();
      if (task) {
        await this.executeTask(task);
      }
      
      // 4. 等待新消息（阻塞）
      await this.messageBus.waitForNewMessage(this.agent.id, timeout: 5000);
    }
  }

  // 处理群聊消息
  async processMessage(msg: Message) {
    // - 如果是@自己，回复
    // - 如果是相关话题，参与讨论
    // - 如果是任务分配，更新状态
  }
}
```

#### IM 消息总线

```typescript
interface IMessageBus {
  // 发送消息到群组
  send(groupId: string, message: Message): Promise<void>;
  
  // 获取未读消息
  getUnread(agentId: string): Promise<Message[]>;
  
  // 阻塞等待新消息
  waitForNewMessage(agentId: string, timeout: number): Promise<void>;
  
  // 获取群聊历史
  getHistory(groupId: string, limit: number): Promise<Message[]>;
}

// 消息结构
interface Message {
  id: string;
  groupId: string;
  senderId: string;
  senderRole: string;
  type: 'chat' | 'task' | 'system' | 'file';
  content: string;
  mentions: string[]; // @的Agent ID列表
  timestamp: Date;
  metadata: {
    taskId?: string;
    filePath?: string;
    codeBlock?: string;
  };
}
```

### 3.3 三分屏界面

```typescript
// 新布局组件
interface ThreePaneLayoutProps {
  // 左上: Agent群聊
  chatPanel: {
    agents: Agent[];
    messages: Message[];
    onSendMessage: (content: string) => void;
    onMentionAgent: (agentId: string) => void;
  };
  
  // 左下: 工作流画布
  canvasPanel: {
    workflow: Workflow;
    onNodeSelect: (node: Node) => void;
    onWorkflowChange: (workflow: Workflow) => void;
    isEditable: boolean; // 审核模式下可编辑
  };
  
  // 右侧: 文件管理
  filePanel: {
    files: ProjectFile[];
    onFileSelect: (file: ProjectFile) => void;
    onFileCreate: (path: string, content: string) => void;
    onFileUpdate: (path: string, content: string) => void;
  };
  
  // 操作栏
  actionBar: {
    status: 'generating' | 'reviewing' | 'executing' | 'completed';
    onApprove: () => void;
    onModify: () => void;
    onReject: () => void;
    onInterrupt: () => void; // 事中干预
  };
}
```

---

## 4. 工作流程详解

### 4.1 完整用户流程

```
[用户] 
  │ 输入: "帮我开发一个Todo App"
  ▼
[Master Agent]
  │ 1. 解析需求: WebApp, 中等复杂度
  │ 2. 规划角色: CTO + PM + 前端 + 后端 + QA
  │ 3. 拆分任务: 
  │    - PM: 编写PRD (串行)
  │    - CTO: 设计架构 (并行)
  │    - 前端/后端: 开发 (依赖架构)
  │    - QA: 测试 (依赖开发)
  │ 4. 生成工作流
  ▼
[审核界面 - 三分屏]
  │ ┌──────────────────────────────┐
  │ │ 左上: Agent团队已创建        │
  │ │ - 🤖 CTO: 等待任务           │
  │ │ - 📝 PM: 等待任务            │
  │ │ - 💻 前端: 等待任务          │
  │ └──────────────────────────────┘
  │ ┌──────────────────────────────┐
  │ │ 左下: 工作流预览             │
  │ │ [Start]→[PM_PRD]→[CTO_Arch] │
  │ │              ↓               │
  │ │    [FE_Dev]→[BE_Dev]        │
  │ │         ↓                    │
  │ │      [QA_Test]→[End]        │
  │ └──────────────────────────────┘
  │ ┌──────────────────────────────┐
  │ │ 右侧: 预计产出文件           │
  │ │ - PRD.md                     │
  │ │ - Architecture.md            │
  │ │ - src/...                    │
  │ └──────────────────────────────┘
  │
  │ 用户操作:
  ├─ [批准并执行] → 进入执行阶段
  ├─ [修改工作流] → 编辑画布后执行
  └─ [添加角色] → 动态添加Agent
  ▼
[执行阶段]
  │ 实时展示:
  │ - 群聊: Agent间协作对话
  │ - 画布: 节点高亮显示进度
  │ - 文件: 实时生成和更新
  │
  │ 用户可随时:
  ├─ 在群聊中@Agent提问
  ├─ 打断执行并修改上下文
  └─ 批准/拒绝Agent的决策
  ▼
[完成]
  │ 交付物:
  │ - 完整代码
  │ - 文档
  │ - 测试报告
  │ - 执行记录
```

### 4.2 Agent 协作流程示例

```
场景: PM编写PRD时遇到需求问题

[PM] 🤔 "这个Todo App需要支持多人协作吗？"
  │
  ▼ (发送到群聊)
[群聊]
  │
  ├─ [CTO] 💬 "建议先支持单用户，v2再加协作"
  │
  ├─ [用户] 💬 "@PM 需要协作，类似Trello"
  │
  └─ [PM] ✅ "收到，我将添加看板视图设计"
  │
  ▼ (更新PRD)
[文件更新]
  PRD.md - 新增"看板视图"章节
```

---

## 5. 数据库 Schema 更新

### 5.1 新增表

```sql
-- Agent角色定义表
CREATE TABLE agent_roles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(50) NOT NULL, -- CTO/PM/前端/后端/QA
  icon VARCHAR(10) NOT NULL, -- 🤖/📝/💻/⚙️/🧪
  system_prompt TEXT NOT NULL,
  capabilities TEXT[], -- 能力标签
  output_artifacts TEXT[], -- 预期产出
  created_at TIMESTAMP DEFAULT NOW()
);

-- Agent实例表
CREATE TABLE agent_instances (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  role_id UUID REFERENCES agent_roles(id),
  name VARCHAR(100),
  status VARCHAR(20), -- idle/busy/completed/error
  assigned_tasks UUID[],
  created_at TIMESTAMP DEFAULT NOW()
);

-- IM消息表
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  group_id VARCHAR(100) NOT NULL, -- 'project:{id}' 或 'task:{id}'
  sender_id UUID REFERENCES agent_instances(id),
  sender_role VARCHAR(50),
  type VARCHAR(20), -- chat/task/system/file
  content TEXT NOT NULL,
  mentions UUID[], -- @的Agent列表
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 项目文件表
CREATE TABLE project_files (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  path VARCHAR(500) NOT NULL,
  content TEXT,
  version INTEGER DEFAULT 1,
  created_by UUID REFERENCES agent_instances(id),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 审核队列表
CREATE TABLE review_queue (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id),
  workflow_id UUID REFERENCES workflows(id),
  status VARCHAR(20), -- pending/approved/rejected/modified
  generated_by VARCHAR(50), -- 'master_agent'
  reviewer_feedback TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  reviewed_at TIMESTAMP
);
```

---

## 6. API 接口设计

### 6.1 新增接口

```typescript
// Master Agent 接口
POST /api/master/analyze
  body: { requirement: string }
  response: { analysis: RequirementAnalysis }

POST /api/master/plan
  body: { analysis: RequirementAnalysis }
  response: { roles: RoleAssignment[], workflow: WorkflowDSL }

// Agent 运行时接口
POST /api/agents
  body: { projectId: string, role: string }
  response: { agentId: string }

GET /api/agents/:projectId
  response: { agents: AgentInstance[] }

// IM消息接口
POST /api/messages
  body: { projectId: string, content: string, mentions?: string[] }
  response: { messageId: string }

GET /api/messages/:projectId
  query: { limit?: number, before?: timestamp }
  response: { messages: Message[] }

WebSocket /ws/projects/:projectId
  // 实时推送: 新消息、Agent状态变更、文件更新

// 审核接口
POST /api/review/approve
  body: { workflowId: string }
  
POST /api/review/reject
  body: { workflowId: string, feedback: string }

POST /api/review/modify
  body: { workflowId: string, modifiedWorkflow: WorkflowDSL }

// 文件管理接口
GET /api/files/:projectId
  response: { files: ProjectFile[] }

POST /api/files
  body: { projectId: string, path: string, content: string }
```

---

## 7. 实施计划

### Phase A: Master Agent 与工作流生成 (Week 1-2)
- [ ] A1: 创建 MasterAgent 核心类
- [ ] A2: 实现需求解析 → 工作流 DSL 生成
- [ ] A3: 角色模板系统 (CTO/产品/开发/QA)
- [ ] A4: 工作流审核与编辑机制

### Phase B: 三分屏界面重构 (Week 3-4)
- [ ] B1: 设计新布局组件 (SplitPane)
- [ ] B2: 左上: Agent 群聊面板
- [ ] B3: 左下: 工作流画布 (现有)
- [ ] B4: 右侧: 项目文件管理

### Phase C: Agent 运行时与通信 (Week 5-6)
- [ ] C1: IM 消息总线实现
- [ ] C2: Agent 自运行循环 (while true)
- [ ] C3: 群聊消息持久化与展示

### Phase D: 集成与优化 (Week 7-8)
- [ ] D1: 完整流程串联
- [ ] D2: 实时状态同步 (WebSocket)
- [ ] D3: 文件生成与展示
- [ ] D4: 性能优化与测试

---

## 8. 与现有系统的兼容性

| 现有组件 | 变更方式 | 说明 |
|---------|---------|------|
| 工作流引擎 | ✅ 保留 | 无需修改，作为执行层 |
| 节点执行器 | ✅ 保留 | 新增 Start/End/Agent 执行器 |
| ReactFlow画布 | 🔄 增强 | 支持 AI 生成布局 + 可编辑模式 |
| useExecution | 🔄 重构 | 改为调用 Master Agent API |
| 数据库 | ➕ 新增表 | 不影响现有表 |
| API路由 | ➕ 新增 | `/api/master/*`, `/api/messages/*` |

---

## 9. 关键决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| AI生成方式 | 一次性生成完整工作流 | 用户审核后再执行，减少不确定性 |
| 布局方案 | 三分屏固定布局 | 群聊、画布、文件都是高频使用区域 |
| 角色定义 | 模板+动态创建 | 预定义常见角色，同时支持自定义 |
| 干预时机 | 先实现事前，再扩展事中 | 事前审核更简单，事中干预需要暂停/恢复机制 |
| 消息存储 | PostgreSQL + Redis | 持久化+实时推送兼顾 |

---

**下一步**: 开始 Phase A 实现 - Master Agent 核心模块
